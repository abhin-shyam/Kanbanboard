Thank you for installing {{ include "kanbanboard.name" . }}!

1) Verify pods:
   oc get pods -l app.kubernetes.io/instance={{ .Release.Name }}

2) Service (ClusterIP):
   oc get svc {{ include "kanbanboard.fullname" . }}

3) Route (external URL):
   oc get route {{ include "kanbanboard.fullname" . }} -o jsonpath='{.spec.host}{"\n"}'

   Open in browser:
   http://$(oc get route {{ include "kanbanboard.fullname" . }} -o jsonpath='{.spec.host}')

If you enabled TLS in values.yaml (route.tls.enabled=true), use https:// instead.
